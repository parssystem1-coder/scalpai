/**
 * FIXTURE - the portal importing a web component. Two apps, two bundles, two
 * deploy units: shared UI belongs in a package, never in a sibling app.
 */
export { DashboardHeader } from "../../web/src/components/DashboardHeader.js";
