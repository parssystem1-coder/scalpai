# تغییرات (Changelog)

این فایل از موج ۳ فعال است و هر نسخهٔ منتشرشده باید این‌جا مدخل داشته باشد؛
نگهبان خودکار آن `scripts/check-changelog-version.cjs` است (تگ = package.json =
مدخل این سند). قالب بر اساس [Keep a Changelog](https://keepachangelog.com/fa/1.1.0/)
و نسخه‌گذاری معنایی است.

## [منتشرنشده]

### بهبود — کادر «بلوغ داده» در بخش یادگیری ماشین

- **بازنویسی کامل متن‌ها به زبان بالینی**: توضیح هر پنج نمودار از زبان مهندسی
  به زبان قابل فهم برای متخصص بازنویسی شد. هر نمودار حالا سه چیز می‌گوید: چه
  چیزی را می‌شمارد، واحدش چیست، و **وقتی سبز شد چه اتفاقی می‌افتد**. راهنمای
  رنگ‌ها هم اضافه شد تا روشن باشد قرمز یعنی «هنوز نه»، نه «خرابی».
- **بخش جدید «حالا چه کنیم؟»**: فهرست شش قدم عملی به‌ترتیب، با مشخص‌کردن مسئول
  هر قدم (شما یا تیم فنی). پیش از سبز شدن نمودارها به‌صورت پیش‌نمایش غیرفعال
  دیده می‌شود تا کاربر بداند چه چیزی در انتظارش است و زودتر از موعد شروع نکند.
- **ثبت نسخهٔ معیار نمره‌دهی در هر تحلیل (AUD-18)**: از این پس هر تحلیل ذخیره
  می‌کند که با کدام نسخه از معیارها ساخته شده. بدون این، اگر روزی معیارها با
  داده‌های واقعی دقیق‌تر شوند، نمرهٔ همان بیمار عوض می‌شد و در نمودار روند
  شبیه «بهبود بالینی» به نظر می‌رسید — در حالی که فقط معیار سنجش تغییر کرده بود.

### رفع اشکال — فاز ۴

- **رفع قفل‌شدن آپلود عکس در حالت نادر (AUD-17)**: اگر فایل تصویر خراب بود یا
  فرمتش پشتیبانی نمی‌شد، مرحلهٔ بررسی «تصویر تکراری» ممکن بود هرگز تمام نشود و
  آپلود برای همیشه روی حالت «در حال بارگذاری» بماند — بدون هیچ پیام خطایی.
  حالا این مرحله حداکثر ۵ ثانیه منتظر می‌ماند و سپس بدون توقف آپلود از آن
  می‌گذرد. این نقص هنگام نوشتن تست‌های دود کشف شد.

### داخلی — فاز ۴ (بدون اثر مستقیم بر کاربر)

- **۲۷ تست دود برای پنج جریان حیاتی کاربر (AUD-13)**: ورود، رضایت‌نامه، ثبت
  مراجع، سد ضدتکرار گالری و زنجیرهٔ تحلیل آفلاین. تا پیش از این هیچ تستی
  رابط کاربری را پوشش نمی‌داد و خرابی این مسیرها فقط در کلینیک معلوم می‌شد.
  مجموع تست‌ها: ۲۷۳ → **۳۰۰**.

### بهبود کیفیت و کارایی — فاز ۳ اصلاحات ممیزی

- **راه‌اندازی سریع‌تر بخش یادگیری ماشین (AUD-14)**: کتابخانهٔ سنگین
  TensorFlow از کد برنامه جدا شد. بستهٔ مربوط به مدل محلی از ۱٬۶۳۳ کیلوبایت به
  **۲۹٫۵ کیلوبایت** رسید و کتابخانه حالا در حافظهٔ نهان باقی می‌ماند؛ یعنی
  به‌روزرسانی‌های بعدی برنامه سبک‌تر دانلود می‌شوند و ورود به بخش یادگیری ماشین
  روی رایانه‌های ضعیف‌تر کلینیک سریع‌تر است.
- **رفع ایرادهای احتمالی «به‌روز نشدن اطلاعات روی صفحه» (AUD-15)**: شش هشدار
  مربوط به وابستگی‌های ناقص در صفحات ورود، مشتریان، جلسات، تقویم و چیدمان اصلی
  به‌صورت ریشه‌ای برطرف شد. جست‌وجوی تأخیردار صفحهٔ مشتریان بدون تغییر رفتار
  اصلاح شد.

### داخلی — فاز ۳ (بدون اثر مستقیم بر کاربر)

- پوشش بررسی خودکار کیفیت به حدود ۶٬۰۰۰ خط کد فرایند اصلی برنامه گسترش یافت
  (AUD-10) و سه ورودی بی‌مصرف و یک re-export مرده حذف شد.
- گارد خودکار «صداقت فنی» برای صفحهٔ دربارهٔ ما (AUD-1): هر ادعای فنی این صفحه
  حالا باید پشتوانهٔ واقعی در کد داشته باشد، وگرنه بررسی‌های خودکار قرمز می‌شوند.
- خط پایهٔ هشدارهای کیفیت کد از ۱۰ به **صفر** رسید.

### انطباق و شفافیت — فاز ۲ اصلاحات ممیزی

- **تب «ردپای حسابرسی» در تنظیمات (AUD-11)**: سیاههٔ رویدادهای حساس — ورود و
  خروج، صدور و بازیابی داده، حذف مراجع و ارسال تصویر به سرویس ابری — حالا برای
  کاربر و بازرس قابل مشاهده است: با تاریخ شمسی، صفحه‌بندی، برجسته‌سازی
  رویدادهای «خروج داده» و خروجی CSV. پیش از این چنین سیاهه‌ای ثبت می‌شد ولی هیچ
  راهی برای دیدنش در برنامه وجود نداشت. خروجی CSV در برابر اجرای فرمول توسط
  اکسل (CSV injection) مقاوم‌سازی شده است.
- **سیاست نگهداری ردپای حسابرسی (AUD-12)**: رویدادهای قدیمی‌تر از ۲۴ ماه و نیز
  مازاد بر ۵۰٬۰۰۰ رویداد، هنگام اجرای برنامه به‌صورت خودکار پاک می‌شوند. پیش از
  این جدول در نسخهٔ SQLite هیچ مرزی نداشت و در چند سال استفاده بی‌نهایت رشد
  می‌کرد. هر دو موتور ذخیره‌سازی حالا از یک سیاست واحد پیروی می‌کنند.
- **غیرفعال شدن پیش‌دستانهٔ دکمهٔ تحلیل آنلاین تا ثبت رضایت‌نامه (AUD-2)**: پیش
  از این کاربر کلیک می‌کرد و *بعد* پیام خطا می‌گرفت؛ حالا دکمه از ابتدا غیرفعال
  است و دلیلش را هم در راهنمای کنار دکمه و هم به‌صورت یک پیام دیدنی توضیح
  می‌دهد. سد منطقی قبلی به‌عنوان لایهٔ دفاع دوم دست‌نخورده باقی مانده است.

### امنیت — فاز ۱ اصلاحات ممیزی (تغییرات محصولی)

- **پسورد فایل پشتیبان اجباری شد (AUD-9)**: وقتی رمزنگاری دادهٔ در سکون فعال
  باشد، دیگر نمی‌توان پشتیبان بدون پسورد ساخت. دلیل: کلید رمز تصاویر برای
  امکان بازیابی روی دستگاه دیگر داخل خودِ بستهٔ پشتیبان قرار می‌گیرد، پس یک
  فایل بدون پسورد عملاً معادل دادهٔ کاملاً رمزنشده بود. گیت در فرایند اصلی
  برنامه اعمال می‌شود (نه فقط در رابط کاربری) و هر چهار مسیر خروجی را پوشش
  می‌دهد. **بازیابی پشتیبان‌های قدیمی بدون پسورد همچنان کار می‌کند** (سازگاری
  عقب‌رو حفظ شد).
- **نشت تصویر بالینی به پوشهٔ موقت سیستم بسته شد (AUD-8)**: تصویری که برای
  تحلیل آفلاین به موتور پایتون داده می‌شود، دیگر در `%TEMP%` عمومی ویندوز
  نوشته نمی‌شود بلکه در پوشهٔ کنترل‌شدهٔ `analyze-tmp` داخل دادهٔ برنامه قرار
  می‌گیرد. پیش از این، کرش یا بسته‌شدن ناگهانی برنامه باعث می‌شد عکس بیمار
  برای همیشه روی دیسک باقی بماند. سه لایهٔ پاک‌سازی اضافه شد: حذف پس از پایان
  تحلیل، پاک‌سازی بقایای جلسات قبلی هنگام اجرای برنامه، و پاک‌سازی هنگام خروج.

## [1.1.0] - 2026-07-30

نخستین نسخه‌ای که زنجیرهٔ انتشار خودکار دارد. شامل اصلاحات سه موج ممیزی-محور
(جزئیات هر موج در `docs/موج-۱-گزارش-اجرا.md`، `docs/موج-۲-گزارش-اجرا.md` و
`docs/موج-۳-گزارش-اجرا.md`).

### اضافه‌شده — موج ۴ (دقت داده‌محور)

- **کارت «کالیبراسیون» در پنل کیفیت مدل**: ECE و Brier روی holdout با رنگ‌بندی
  نقشه‌راه (≤0.10 سبز / ۰٫۱۰–۰٫۲۰ کهربانی / >0.20 قرمز) + tooltip فارسی، CI95
  (MAE و F1 ماکرو از K-Fold مشتری‌محور)، و تعداد نمونهٔ holdout. مدل‌های قدیمی
  بدون عدد جعلی: پیام «بازآموزی کنید» نمایش داده می‌شود.
- **فلگ «ارزیابی حداقلی»**: وقتی کمتر از ۳ مشتری متمایز وجود دارد، هر دو fallback
  اسپلیت (holdout خالی یا K-Fold با validation=holdout) در متادیتا ذخیره و در UI
  با بنر کهربانی «دادهٔ مشتری‌محور کافی نیست» هشدار داده می‌شوند.
- **Temperature Scaling واقعی و مشروط**: اسکالر T فقط وقتی برازش و پذیرفته می‌شود
  که holdout ≥ ۶۰ نمونه، ECE قبل > 0.10 و validation ≥ ۳۰ نمونه باشد؛ T با
  کمینه‌سازی NLL (golden-section روی log T) روی validation پیدا و فقط با بهبود
  واقعی ECE روی holdout پذیرفته می‌شود. در صورت پذیرش: sigmoid(logit/T) فقط روی
  ۶۶ خروجی برچسب در predict اعمال می‌شود، آستانه‌های برچسب روی احتمال دماشدهٔ
  validation بازکالیبره می‌شوند، و گزارش A/B (ECE قبل/بعد + دلیل فارسی) در
  متادیتا ذخیره و در کارت کالیبراسیون نمایش داده می‌شود.
- **لایهٔ شواهد کالیبراسیون heuristic**: پس از سبز شدن گیج ۱۵۰ نمونهٔ متخصص،
  گزارش قبل/بعد MAE برای هر ۹ امتیاز با K-Fold تکرارشونده (۳×۵) روی نگاشت‌های
  isotonic (PAVA) و خطی دوم پارامتری — صرفاً «شواهد»؛ اعمال نهایی به ضرایب مشترک
  به حلقهٔ parity عددی سه موتور و رضایت مالک موکول شده است.
- **باقی‌ماندهٔ موج**: CLAHE + بامپ نسخهٔ فیچر (D4) به‌علت نبود حلقهٔ parity
  عددی TS↔Python (معادل cv2.createCLAHE) موکول شد؛ مستندات در
  `docs/موج-۴-گزارش-اجرا.md`.

### اضافه‌شده — موج ۳ (عملیات و توزیع)

- **به‌روزرسانی خودکار**: `electron-updater` + انتشار GitHub Releases با تگ
  `v*` (ورک‌فلوی آمادهٔ `docs/release-pipeline/release.yml` — فعال‌سازی با یک
  کپی توسط مالک به دلیل مجوز GitHub App، راهنما در `docs/release.md` §۰)؛ بنر
  فارسی/انگلیسی در اپ با دکمهٔ «ری‌استارت و نصب»؛ چکِ خودکارِ تأخیری بعد از
  لانچ و چکِ دستی؛ در محیط توسعه کاملاً غیرفعال.
- **پشتیبان‌گیری فایل‌محور**: خروجی/بازیابی بکاپ دیگر payload باینری را روی IPC
  نمی‌فرستد؛ ساخت ZIP به‌صورت استریم در main و روی دیسک انجام می‌شود (بکاپ
  خودکار روزانه هم همین مسیر). تست پروفایل حافظه: `scripts/test-backup-stream.cjs`.
- **مدل محلی داخل بکاپ**: وزن‌های مدل TF.js به‌صورت اختیاری داخل فایل پشتیبان
  می‌نشیند؛ هنگام بازیابی، مدل به‌عنوان «چلنجر» غیرفعال در تب یادگیری ماشین
  نمایش داده می‌شود و فقط با تأیید دستی کاربر فعال می‌گردد.
- **آمادگی امضای کد ویندوز**: ورک‌فلوی ریلیز سکرت‌های `CSC_LINK` /
  `CSC_KEY_PASSWORD` را می‌پذیرد؛ راهنمای خرید و نصب گواهی در `docs/release.md`.
- **قفل نسخه**: مدخل CHANGELOG اجباری و بررسی تطابق تگ با `package.json`.

### امنیت — موج ۲ (رمزنگاری دادهٔ در سکون)

- رمزنگاری دیتابیس (SQLCipher) با کلید در safeStorage سیستم‌عامل + کلید بازیابی
  قابل مشاهده با احراز هویت؛ رمزنگاری تصاویر (AES-256-GCM) با مهاجرت درون‌برنامه‌ای
  تصاویر قدیمی؛ رضایت‌نامهٔ حریم‌خصوصی نسخه‌دار؛ ردپای حسابرسی رویدادهای حساس؛
  خروجی رمزدار v4 با پسورد دلخواه کاربر.

### اصلاح‌شده — موج ۱ (اتصال و قابلیت اعتماد)

- اتصال ماژول‌های یتیم به ناوبری، dHash گالری برای تشخیص تصویر تکراری، بازمحاسبهٔ
  فیچر نمونه‌های قدیمی از تصویر خام، سخت‌سازی مجوزهای چاپ و مسیرهای خروجی.

## [1.0.0]

نسخهٔ عمومی اولیه — پیش از ممیزی سه‌موجی. روی GitHub Release منتشر نشده بود.
