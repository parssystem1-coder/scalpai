# فاز ۳ — گزارش اجرا (سنجش کیفیت و سخت‌سازی تحلیل آنلاین)

> تغییرات **واقعی و اعمال‌شده**. روی برنچ `arena/019fa5a1-scalpai`.

## ۳٫۱ — پنل «توافق AI آنلاین با متخصص» (مهم‌ترین آیتم فاز)

**شکافی که پر شد:** پروژه تا امروز هیچ عدد واقعی دربارهٔ دقت تحلیل آنلاین
نداشت. تنها سیگنال موجود «تأیید/عدم تأیید» باینری بود که نمی‌گفت AI *چقدر* و
*در کدام شاخص* اشتباه می‌کند.

**زیرساخت داده:**
- دو ستون جدید `originalAiLabel` و `originalAiLabelAt` روی `training_samples`،
  با migration نسخهٔ ۹ (`addColumnIfMissing`، سازگار با دیتابیس‌های موجود).
- **قاعدهٔ write-once در لایهٔ دیتابیس پیاده شد، نه در UI.** وقتی متخصص یک
  نمونهٔ `online_ai` را به `expert` تصحیح می‌کند، برچسب قبلی به‌طور خودکار
  به‌عنوان baseline ثبت می‌شود و **ویرایش‌های بعدی آن را بازنویسی نمی‌کنند**.
  اگر این قاعده در UI بود، ویرایش دوم baseline را خراب می‌کرد و «توافق AI»
  به‌تدریج و ساختگی بهبود پیدا می‌کرد.
- هر سه بک‌اند (`sqlite`، `json`، `IndexedDB وب`) دقیقاً همین رفتار را دارند تا
  از هم واگرا نشوند. مسیرهای export/import هم round-trip می‌کنند.

**سنجش:** ماژول خالص `src/lib/aiAgreement.ts` — MAE و سوگیری علامت‌دار هر شاخص
(«AI بیش‌برآورد می‌کند یا کم‌برآورد؟»)، و precision/recall/F1 مجموعهٔ تشخیص‌ها
با تفکیک «فقط AI» (مثبت کاذب) از «فقط متخصص» (موردی که AI ندیده).

**صداقت آماری:** پنل صراحتاً می‌گوید این آمار **سوگیری انتخاب** دارد (معمولاً
موارد مشکوک‌تر بازبینی می‌شوند)، و وقتی داده‌ای نیست «هنوز داده کافی نیست»
نمایش می‌دهد نه «دقت صفر». همچنین توضیح می‌دهد نمونه‌های قبل از این به‌روزرسانی
baseline ندارند و در گزارش نمی‌آیند.

## ۳٫۲ — ثبت شناسهٔ مدل پاسخ‌دهنده

`extractResponseModelId()` برای هر دو پروتکل (Gemini و OpenAI-compatible).
مهم است چون روترهایی مثل OpenRouter می‌توانند درخواست را به مدل دیگری — گاهی
بدون قابلیت vision — بفرستند؛ بدون این فیلد نمی‌شد کیفیت ضعیف را به مدل نسبت
داد یا از پرامپت تفکیک کرد. در `AIAnalysisResult.responseModelId` ذخیره می‌شود.

## ۳٫۳ — تلاش مجدد با پس‌رفت نمایی

- `isTransientAiError()` فقط 5xx و خطاهای شبکه/timeout را گذرا می‌داند.
  **401/403 و 429 عمداً retry نمی‌شوند** — تکرارشان فقط سهمیه می‌سوزاند و کاربر
  را بی‌دلیل منتظر نگه می‌دارد.
- `backoffDelayMs()` با jitter کامل (بازهٔ نصف تا کامل) تا درخواست‌های هم‌زمان
  روی هم نیفتند.
- در **هر دو مسیر** پیاده شد: مرورگر (`aiProvider.ts`) و Electron
  (`ai-handlers.cjs`). لغو توسط کاربر هرگز retry نمی‌شود.

## ۳٫۴ — سخت‌سازی پرامپت در برابر تزریق

متن آزاد بیمار (حساسیت‌ها/سبک زندگی/یادداشت) مستقیم وارد پرامپت می‌شد. حالا:
- `sanitizePatientFreeText()` تلاش برای بستن حصار (`</PATIENT_DATA>`) و
  نشانگرهای نقش چت (`<|im_start|>`، `system:`) را خنثی می‌کند.
- کل بلوک دادهٔ بیمار داخل `<PATIENT_DATA>` حصار شد و دستور صریح امنیتی بالای
  پرامپت اضافه شد: محتوای داخل حصار **داده** است نه دستور.
- تست تأیید می‌کند متن بالینی عادی دست‌نخورده می‌ماند (بدون آسیب به داده‌های
  واقعی بیمار).

## ۳٫۵ — تشخیص پاسخ مشکوک به «ندیدن تصویر»

`detectGenericAiResponse()` نشانه‌هایی مثل امتیازهای کاملاً یکسان، اطمینان یکسان
روی همهٔ ضایعات، و خالی‌بودن هم‌زمان تشخیص‌ها و توصیه‌ها را می‌سنجد.
**آستانه عمداً «حداقل دو نشانه» است** — یک نشانه به‌تنهایی می‌تواند کاملاً طبیعی
باشد (مثلاً تصویر واقعاً سالم است و ضایعه‌ای ندارد). هشدار قرمز در تب نتایج.

## تأییدیهٔ کیفیت (همه واقعاً اجرا شد)

| بررسی | نتیجه |
|---|---|
| `tsc -b` | ✅ |
| `eslint .` | ✅ ۰ خطا (همان ۱۰ هشدار قدیمی) |
| `vitest run` | ✅ **۱۴۳/۱۴۳** (۱۱۸ قبلی + ۲۵ تست جدید) |
| `test-db-contract.cjs` | ✅ sqlite و json |
| `check-shared-constants.cjs` | ✅ |
| `vite build` (prod) | ✅ |
| اجرای واقعی migration روی دیتابیس نسخهٔ ۸ | ✅ ستون‌ها اضافه شد، رکورد قدیمی سالم ماند |
| اجرای واقعی write-once روی بک‌اند JSON | ✅ baseline در ویرایش اول ثبت و در ویرایش دوم حفظ شد |

## محدودیت صادقانه

سنجش ۳٫۱ از امروز شروع به جمع‌آوری داده می‌کند؛ برای نمونه‌های گذشته baseline
وجود ندارد و در گزارش نمی‌آیند. این در UI هم به کاربر گفته می‌شود.
